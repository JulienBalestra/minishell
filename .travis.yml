os:
  - osx
  - linux

language: c

sudo: required

before_install:
  - brew update || apt-get update

install:
  -  brew install valgrind || apt-get install valgrind

script:
  - ./setup.sh
  - ./run_test.sh