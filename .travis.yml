os:
  - osx
  - linux

language: c

sudo: required

before_install:
  - sudo brew update || sudo apt-get update

install:
  -  sudo brew install valgrind || sudo apt-get install valgrind

script:
  - ./setup.sh
  - ./run_test.sh